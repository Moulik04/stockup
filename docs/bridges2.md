# PSC Bridges-2 (Phase 7, stretch)

_Placeholder._ Deep models (N-BEATS / PatchTST) only, fp16 only (no bf16, no FlashAttention on
V100). Module names and SLURM specifics need verifying against PSC docs before any
`scripts/bridges2/*.sbatch` is written.
